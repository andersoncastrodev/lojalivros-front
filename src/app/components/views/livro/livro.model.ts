export interface Livro{
    
    /*Esses campos devem ser os mesmo lá do domian da API - IMPORTANTE ISSO. */
    id?: String;

    titulo: String;

    nomeAutor: String;

    texto: String;
}